- message ||= nil


- provide(:title, page_title(t('views.errors.bad_request.title')))

- provide(:page_type, 'error')


- content_for :header do
  div.header-head
    div.container.centerize
      h1 = t('views.errors.bad_request.message')
      - if message
        p = message
